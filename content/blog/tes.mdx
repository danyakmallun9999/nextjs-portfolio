---
title: 'tes'
description: 'tes'
publishedAt: '2025-12-25'
coverImage: '/posts/efek-kupu-kupu/kupu-kupu-3.png'
category: 'Self-Development'
tags:
  [
    'keputusan',
    'efek kupu-kupu',
    'opportunity cost',
    'sun tzu',
    'kesadaran diri',
    'pengembangan diri',
  ]
---

Baik, ini contoh artikel **Markdown** yang bisa kamu pakai buat ngetes `react-syntax-highlighter`. Isinya lumayan lengkap: heading, list, table, gambar, dan beberapa code block dengan berbagai bahasa.

Silakan taruh file ini di folder blog kamu, misalnya `test-syntax-highlighter.md`.

---

# Testing React Syntax Highlighter

Ini adalah artikel uji coba untuk memastikan fitur **highlighting kode** di blog kamu berjalan dengan mulus. Kalau halaman ini tampil tanpa error dan blok kodenya berwarna, berarti setup kamu aman.

## JavaScript

```javascript
function greet(name) {
  return `Hello, ${name}!`
}

console.log(greet('Dany'))
```

## TypeScript

```typescript
interface User {
  id: number
  name: string
}

const user: User = {
  id: 1,
  name: 'Dany',
}

console.log(user)
```

## PHP

```php
<?php

function hello($name) {
    return "Hello, $name";
}

echo hello("Dany");
```

## HTML

```html
<div class="container">
  <h1>Hello World</h1>
  <p>Testing syntax highlighting.</p>
</div>
```

## SQL

```sql
SELECT *
FROM users
WHERE status = 'active'
ORDER BY created_at DESC;
```

## JSON

```json
{
  "name": "Dany",
  "role": "Developer",
  "active": true
}
```

## Python

```python
def add(a, b):
    return a + b

print(add(10, 20))
```

## List

- Test item 1
- Test item 2
- Test item 3

## Table

| Bahasa     | Status |
| ---------- | ------ |
| JavaScript | OK     |
| TypeScript | OK     |
| PHP        | OK     |

## Gambar (Testing Next.js Markdown Image)

![Testing Image](https://placehold.co/600x300)

---

Kalau ini tampil mulus dan code block-nya berwarna sesuai tema `a11yDark`, berarti syntax highlighter kamu sudah sukses.
